using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SliderRod : MonoBehaviour
{
/*    private Slider MoveSlider;
    public GameObject IronROD;
    public Animator IronRodAnimation;

    void Start()
    {
        MoveSlider = GetComponent<Slider>(); 
    }

    public void ApplyAnimation()
    {
        if(IronRodAnimation != null)
        {
            Animator IronRodAnimation = IronROD.GetComponent<Animator>();
        }
        if (IronRodAnimation)
        {
            IronRodAnimation.SetFloat("RodVal", MoveSlider.value);
        }
    }
*/
}